import "../styles/Header.css"

export default function Header() {
    return (
        <header className="header-wrapper">
            <h2 className="header-text">mytodolist.</h2>
        </header>
    )
}